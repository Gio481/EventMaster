apply plugin: "EMGradleConfigPlugin"
apply plugin: "EMComposePlugin"
apply plugin: "EMGradleExtensionPlugin"

configPlugin {
    config {
        buildTypes = [BuildTypes.DEBUG, BuildTypes.TEST, BuildTypes.PRODUCTION]
    }
}

android {
    namespace 'com.eventmaster.splash.impl'
}

dependencies {
    implementation project(':core:EMCore-Navigation')
    implementation project(':presentation:EventMasterSplash:Api')
    implementation project(':presentation:EventMasterHome:Api')
}