pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

include ':app'

include ':EMCore-Navigation'

include ':EMSplash-Api'
include ':EMSplash-Impl'

include ':EMHome-Api'
include ':EMHome-Impl'

rootProject.name = "EventMaster"