pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

include ':app'

include ':core:EMCore-Navigation'
include ':core:EMCore-UI'

include ':data:EventMasterSplash'
include ':data:EventMasterHome'

include ':domain:EventMasterSplash'
include ':domain:EventMasterHome'

include ':presentation:EventMasterSplash:Api'
include ':presentation:EventMasterSplash:Impl'

include ':presentation:EventMasterHome:Api'
include ':presentation:EventMasterHome:Impl'

include ':util:EventMasterDashboard'

rootProject.name = "EventMaster"