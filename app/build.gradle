apply plugin: "EMGradleSetUpPlugin"
apply plugin: "EMComposePlugin"

android {
    namespace 'com.eventmaster'

    defaultConfig {
        applicationId "com.example.eventmaster"
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        "$BuildTypes.PRODUCTION" {}
        "$BuildTypes.TEST" {}
        "$BuildTypes.DEBUG" {
            applicationIdSuffix ".debug"
            versionNameSuffix "-debug"
            debuggable true
            signingConfig signingConfigs.debug
        }
    }

    applicationVariants.all { variant ->
        def buildType = variant.buildType.name
        def values = fieldValues
        variant.buildConfigField "String", "APP_BASE_URL", "\"${values[buildType]?.baseUrl ?: ''}\""
    }
}

dependencies {
    implementation project(':Core:Navigation')
    implementation project(':Core:Presentation')
    implementation project(':Core:Api')

    implementation project(':Features:Splash:Api')
    implementation project(':Features:Splash:Impl')
    implementation project(':Features:Authentication:Api')
    implementation project(':Features:Authentication:Impl')
    implementation project(':Features:Event:Api')
    implementation project(':Features:Event:Impl')
    implementation project(':Features:Group:Api')
    implementation project(':Features:Group:Impl')
    implementation project(':Features:Home:Api')
    implementation project(':Features:Home:Impl')

    implementation libs.ktx
    implementation platform(libs.kotlin)
    implementation libs.lifecycle.runtime
    implementation libs.compose.activity
    implementation libs.gson
}